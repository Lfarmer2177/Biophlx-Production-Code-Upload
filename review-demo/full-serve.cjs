const http=require('http'),fs=require('fs'),path=require('path');
const root=path.resolve(__dirname,'../../../outputs/full-app-demo');
http.createServer((req,res)=>{let name;try{name=decodeURIComponent(new URL(req.url,'http://localhost').pathname).slice(1)||'index.html';}catch{return res.writeHead(400).end();}const target=path.resolve(root,name);if(!target.startsWith(root+path.sep)||!fs.existsSync(target)||fs.statSync(target).isDirectory())return res.writeHead(404).end();const size=fs.statSync(target).size;res.setHeader('Content-Type',({'.html':'text/html','.js':'text/javascript','.mp4':'video/mp4','.png':'image/png'})[path.extname(target)]||'application/octet-stream');res.setHeader('Accept-Ranges','bytes');const range=req.headers.range?.match(/^bytes=(\d+)-(\d*)$/);if(range){const start=Number(range[1]),end=Math.min(range[2]?Number(range[2]):size-1,size-1);if(start>end||start>=size)return res.writeHead(416,{'Content-Range':`bytes */${size}`}).end();res.writeHead(206,{'Content-Range':`bytes ${start}-${end}/${size}`,'Content-Length':end-start+1});fs.createReadStream(target,{start,end}).pipe(res);}else{res.setHeader('Content-Length',size);fs.createReadStream(target).pipe(res);}}).listen(4174,'127.0.0.1',()=>console.log('Demo: http://127.0.0.1:4174'));
